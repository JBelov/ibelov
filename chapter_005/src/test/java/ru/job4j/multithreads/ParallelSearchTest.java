package ru.job4j.multithreads;

import org.junit.Test;

import java.util.Arrays;
import java.util.List;

import static org.hamcrest.core.Is.is;
import static org.junit.Assert.assertThat;

/**
 * @author Ivan Belov (ivan@belov.org)
 * @version $Id$
 * @since 0.1
 */

public class ParallelSearchTest {
    @Test
    public void whenTextFound() {
        /*
        ParallelSearch search = new ParallelSearch("/Users/belov/Desktop/", "someText", Arrays.asList(".txt"));
        search.init();
        List<String> expected = Arrays.asList("/Users/belov/Desktop/hereitis.txt", "/Users/belov/Desktop/heretoo.txt");
        assertThat(search.result(), is(expected));
         */
    }
}